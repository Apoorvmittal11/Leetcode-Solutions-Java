https://leetcode.com/problems/distribute-candies/description/
class Solution {
    public int distributeCandies(int[] c) {
        Arrays.sort(c);
        if(c[c.length-1]==c[0]){
            return 1;
        }
        return c.length/2;
    }
}
